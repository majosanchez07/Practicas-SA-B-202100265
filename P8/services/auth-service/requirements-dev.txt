# Dependencias exclusivas de las pruebas.
# Se mantienen separadas de requirements.txt para que la imagen de produccion
# no las arrastre: el Dockerfile.prod solo instala requirements.txt.
pytest>=8.3.0
pytest-cov>=5.0.0
httpx>=0.27.0
